# History — Project Gutenberg

Place plain-text Gutenberg history texts in this directory.

Recommended naming: `pg<ID>_<title>.txt`

After adding books, run from the repo root:

```bash
kgrag init books/history --layer doc --name gutenberg-history
```

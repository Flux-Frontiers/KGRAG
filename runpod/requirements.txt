# RunPod KGRAG worker — PyPI dependencies
# Non-PyPI packages (kg-rag, gutenberg-kg, metabo-kg) are installed
# from local wheels by build_image.sh before docker build runs.

runpod>=1.7.0
# kgmodule-utils is pulled transitively by doc-kg, but is pinned explicitly so
# the floor is declared rather than inherited: an unpinned transitive is how
# the published image ended up shipping 0.2.4 against a fleet at 0.10.0.
# The [semantic] extra arrives via doc-kg; nothing here needs the others.
kgmodule-utils>=0.10.0
doc-kg>=0.21.0
sentence-transformers>=5.4.1
# Still required: handler.py registers the Gutenberg and MetaboKG entries with
# lancedb_path=, and neither of those packages declares lancedb itself. doc-kg
# >=0.18.2 moved its own vectors to sqlite-vec, but the volume-mounted Gutenberg
# and Metabo indices remain LanceDB-backed.
lancedb>=0.29.0
numpy>=1.24.0
httpx>=0.27.0
rich>=13.0.0,<15.0.0
click>=8.1.0
pandas>=2.0.0
boto3>=1.34.0

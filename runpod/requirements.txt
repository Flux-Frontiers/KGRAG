# RunPod KGRAG worker — PyPI dependencies
# Non-PyPI packages (kg-rag, gutenberg-kg, metabo-kg) are installed
# from local wheels by build_image.sh before docker build runs.

runpod>=1.7.0
doc-kg>=0.15.2
sentence-transformers>=5.4.1
lancedb>=0.29.0
numpy>=1.24.0
httpx>=0.27.0
rich>=13.0.0,<15.0.0
click>=8.1.0
pandas>=2.0.0
boto3>=1.34.0

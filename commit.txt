chore: extract agent_kg to dedicated repo, add as external dependency

Remove bundled src/agent_kg/ — the package now lives at
Flux-Frontiers/agent_kg and is pulled in as a git dependency.
Drop agent-kg script entries and agent-kg-nlp extra from pyproject.toml.
Add pylint disable for lazy imports in agent_adapter.py.
Add agent-kg.skill, settings templates, and .mcp.json registrations.

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
